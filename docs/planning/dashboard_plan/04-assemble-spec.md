# Assemble docs/dashboard_spec.md

- **Type**: `wayfinder:task` (HITL)
- **Status**: open
- **Assignee**: unclaimed
- **Blocked by**: [The explanatory layer](01-explanatory-layer.md), [The comparison view](02-comparison-view.md), [The rerun workflow](03-rerun-workflow.md)
- **Part of**: [Map: Dashboard next iteration](../dashboard_plan.md)

## Question

Nothing to decide. The terminus: gather the resolutions from every closed ticket into
`docs/dashboard_spec.md` — decisions, rationale, and the comparison mock — then add a
one-line pointer under a new `### Dashboard` heading in `AGENTS.md`.

The map closes when this is done. Implementation is a separate effort, as is the QA
tab, which waits on [Map: Model semantics](../model_plan.md).
