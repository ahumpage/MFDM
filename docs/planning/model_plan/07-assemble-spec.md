# Assemble docs/model_semantics.md

- **Type**: `wayfinder:task` (HITL)
- **Status**: open
- **Assignee**: unclaimed
- **Blocked by**: [The QA pass](05-qa-report.md), [Test fixtures](06-test-fixtures.md)
- **Part of**: [Map: Model semantics](../model_plan.md)

## Question

Nothing to decide. The terminus: gather the resolutions from every closed ticket into
`docs/model_semantics.md` — decisions, rationale, and the worked numbers for the
awkward hours — then add a one-line pointer under a new `### Model semantics` heading
in `AGENTS.md`.

The map closes when this is done. Implementation is a separate effort.
